
---
apiVersion: v1
kind: Service

metadata:
  name: fb-svc

spec:
  type: LoadBalancer

  selector:
    app: facebook
    env: prod
    tier: frontend

  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
